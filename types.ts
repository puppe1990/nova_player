export const MAX_VIDEOS = 4;

export interface VideoMetadata {
  name: string;
  size: string;
  type: string;
  url: string;
}

export interface AudioMetadata {
  name: string;
  size: string;
  type: string;
  url: string;
}
